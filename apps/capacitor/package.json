{
  "name": "@arceus/capacitor",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -s -F @arceus/web build:vite --mode dev-capacitor && pnpm -F @arceus/web build:mock && run-p mock:start android:start",
    "build": "pnpm -s -F @arceus/web build && cap sync android && android\\gradlew.bat -p android assembleDebug",
    "mock:start": "pnpm -F @arceus/web mock:start",
    "android:start": "cap run android",
    "icon": "capacitor-assets generate --android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.2",
    "npm-run-all2": "catalog:"
  }
}
